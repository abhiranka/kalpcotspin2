export function formatWeight(weight) {
  return weight ? `${weight}` : "-";
}

export function formatWidth(width) {
  return width ? `${width}"` : "-";
}

export function formatShrinkage(value) {
  return value || "-";
}